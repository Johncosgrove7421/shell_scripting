#!/bin/bash

pwd = $1

touch /$1/shopping_cart.sh
touch /$1/ll.sh
touch /$1/brew.sh
touch /$1/wget.sh
touch /$1/vs_code.sh
touch /$1/git.sh
touch /$1/python3.sh
touch /$1/terraform.sh
touch /$1/kubectl.sh
touch /$1/kubectl_bash_completion.sh  #optional to auto-complete kubectl commands
touch /$1/docker.sh
touch /$1/aws_cli.sh
touch /$1/key_gen.sh    #generated keys to import into your IAM user account
touch /$1/helm.sh 
touch /$1/opening_repo.sh # this a basic script to open your repo in vs code
touch /$1/iterm2.sh     #optional for advanced terminal features
touch /$1/tree.sh    #tree is a plugin to view multiple folder contents at one time
touch /$1/watch.sh   #watch is a good command to view kubectl resource come up
touch /$1/oh_my_zsh.sh    #optional terminal feature to add some extra features and 
# looks, for more information view the following link: 
# https://www.howtogeek.com/444596/how-to-change-the-default-shell-to-bash-in-macos-catalina/
touch /$1/minikube.sh   #For Kubernetes labs / learning
touch /$1/neofetch.sh  #optional for terminal display
touch /$1/spaceship.sh   #optional to add more color to your boring terminal
